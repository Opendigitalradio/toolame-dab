language: c

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libvlc-dev vlc-nox libjack-jackd2-dev libzmq3-dev libzmq3

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make
